spring:
  application:
    name: discovery-service
server:
  port: 8761
eureka:
  dashboard:
    enabled: true
  client:
    register-with-eureka: false
    fetch-registry: false
  server:
    renewal-percent-threshold: 0.85